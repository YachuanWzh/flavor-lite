{
  "_comment": "Copy this file to <workspace>/.flavorlite/memory/embedding.json and fill in your endpoint. The plugin reads that path first; the manifest config.embedding is only a fallback. While unset, memory recall runs BM25-only.",
  "url": "https://api.openai.com/v1/embeddings",
  "model": "text-embedding-3-small",
  "apiKey": "",
  "timeoutMs": 15000,
  "batchSize": 8
}
